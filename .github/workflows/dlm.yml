# Builds DLM firmware

name: dlm

on:
  workflow_dispatch:
  push:
    paths:
    - 'components/dlm/**'

jobs:
  dlm-build:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: docker-compose run dlm