# Builds DAM firmware

name: dam

on:
  push:
    paths:
    - 'components/dam/**'

jobs:
  dam-build:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: docker-compose run dam