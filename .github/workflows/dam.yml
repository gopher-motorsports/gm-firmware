# Builds DAM firmware

name: DAM

on:
  push:
    paths:
    - 'components/dam'

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: docker-compose run dam